#!/usr/bin/env bash
set -euo pipefail
shopt -s nullglob
candidates=("$(cd "$(dirname "$0")/.." && pwd)"/lib/liqi_platform-*/priv/drain.sh)
[[ ${#candidates[@]} -eq 1 ]] || { echo "expected exactly one LIQI drain provider script" >&2; exit 66; }
exec "${candidates[0]}" "$@"
